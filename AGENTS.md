# AGENTS.md

- 我们开发的所有内容必须要可以在 mac 和 win 双端运行。
- 所有字符集优先 utf-8 显示声明。
- 首先阅读 `.codegraph` 文件夹，直接从里面获取代码结构和依赖，然后根据需求去读取你需要的内容。
- 如果架构和代码有修改，请自动运行 `codegraph init -i` 进行 codegraph 重新初始化。
- 修改后，请同步扫描所有关联项，确认全部修改到位。
- 需要用户决策的内容全部翻译为中文输出。
- 始终以现有代码为最终依据，不要轻信文档内容，可能不是最新版。
- 代码风格需要始终保持一致。
- 文件打开和保存依赖浏览器 File System Access API 是当前产品的刻意设计决策，不作为 bug 或缺陷处理；除非用户明确要求兼容非 Chromium 浏览器，否则不要把这一点列为问题。
